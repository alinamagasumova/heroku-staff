<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <header>
        <div>
            <a href="/">logo</a>
            <ul>
                {{#each links}}
                    <li><a href="/page/{{this}}">{{this}}</a></li>
                {{/each}}
            </ul>
        </div>
    </header>
    <main>
        {{{body}}}
        {{!-- 3 уса - вставка шаблона --}}
    </main>
</body>
</html>